import { Component, EventEmitter, Input, Output } from '@angular/core';
@Component({ selector: 'app-clientes-dialog', template: `<p-dialog header="Búsqueda de Clientes" [(visible)]="visible" [modal]="true" [style]="{width:'60vw'}" (onHide)="close()" role="dialog"><div class="p-4"><div class="p-inputgroup mb-3"><input pInputText [(ngModel)]="searchValue" placeholder="Buscar cliente"><button pButton icon="pi pi-search" (click)="onSearch()"></button></div></div><ng-template pTemplate="footer"><button pButton label="Cerrar" (click)="close()"></button></ng-template></p-dialog>` })
export class ClientesDialogComponent { @Input() visible = false; @Output() visibleChange = new EventEmitter<boolean>(); searchValue = ''; onSearch(): void {} close(): void { this.visible = false; this.visibleChange.emit(false); } }
