import { Component, EventEmitter, Input, Output } from '@angular/core';
@Component({ selector: 'app-envio-mensaje-dialog', template: `<p-dialog header="Envío de Mensaje de Texto" [(visible)]="visible" [modal]="true" [style]="{width:'50vw'}" (onHide)="close()" role="dialog"><div class="grid p-4"><div class="col-12"><label>Teléfono</label><input pInputText [(ngModel)]="telefono" class="w-full"></div><div class="col-12"><label>Mensaje</label><textarea pInputTextarea [(ngModel)]="mensaje" [rows]="4" class="w-full"></textarea></div></div><ng-template pTemplate="footer"><button pButton label="Enviar" icon="pi pi-send" (click)="onSend()"></button><button pButton label="Cerrar" (click)="close()"></button></ng-template></p-dialog>` })
export class EnvioMensajeDialogComponent { @Input() visible = false; @Output() visibleChange = new EventEmitter<boolean>(); telefono = ''; mensaje = ''; onSend(): void { this.close(); } close(): void { this.visible = false; this.visibleChange.emit(false); } }
