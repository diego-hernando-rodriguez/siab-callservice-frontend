import { Component, EventEmitter, Input, Output } from '@angular/core';
@Component({ selector: 'app-acuerdos-clientes-dialog', template: `<p-dialog header="Acuerdos de Clientes" [(visible)]="visible" [modal]="true" [style]="{width:'60vw'}" (onHide)="close()" role="dialog"><div class="p-4"><p>Acuerdos de clientes</p></div><ng-template pTemplate="footer"><button pButton label="Cerrar" (click)="close()"></button></ng-template></p-dialog>` })
export class AcuerdosClientesDialogComponent { @Input() visible = false; @Input() llamadaNumero: number | null = null; @Output() visibleChange = new EventEmitter<boolean>(); @Input() data: any = {}; close(): void { this.visible = false; this.visibleChange.emit(false); } }
